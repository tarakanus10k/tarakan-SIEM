logs:
  - type: file
    name: syslog
    path: /var/log/syslog

  - type: file
    name: authlog
    path: /var/log/auth.log

  - type: journald
    name: systemjournal
    # Optional
    # services: ["sshd.service", "cron.service"]